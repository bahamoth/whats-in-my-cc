node_modules/
dist/
.vite/
*.log
*.tsbuildinfo
# vite.config.js/.d.ts를 여기 넣지 않는다 — vite는 .ts 설정을 직접 읽으므로
# js 산출물이란 단계 자체가 없고(전 tsconfig noEmit), 생긴다면 비정상이라
# git status에 보여야 한다. 커밋 유입은 noStaleViteConfig.test.ts 게이트가
# CI에서 차단한다 (2026-07-06 대시보드 렌더 실사고).
