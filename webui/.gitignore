node_modules/
dist/
.vite/
*.log
*.tsbuildinfo
# tsc 오발 emit 산출물 — 커밋 금지 대상이라 ignore한다. 단 존재만으로 위험:
# vite는 .js 설정을 .ts보다 먼저 집어 alias 없는 낡은 설정으로 조용히
# 기동한다(2026-07-06 대시보드 렌더 실사고). 존재 감지는
# src/__tests__/noStaleViteConfig.test.ts 게이트가 맡는다.
vite.config.js
vite.config.d.ts
