/target
**/*.rs.bk
.wimcc.sqlite*
payloads/
.DS_Store
webui/node_modules/
webui/dist/

# Node + Vite caches (root-level vitest, webui, anywhere)
node_modules/
.vite/

# superpowers brainstorm mockups
.superpowers/
.wimcc-analysis.sqlite*

# plugin marketplace manifests — un-ignore from the global `.claude*` pattern
# (~/.gitignore); these MUST ship with the repo for `/plugin marketplace add`.
!.claude-plugin/

# serena cache
.serena/

# dogfood 원장은 형상관리 제외 (2026-07-09 사용자 결정: git 추적은
# 크로스 워크스페이스 이어작업 용도였는데 그 필요가 종료됐다. 로컬 파일은
# git rm --cached로 디스크에 남으므로 session-retrospect 스킬은 이 머신에서
# 계속 동작한다 — 사라지는 건 리포 히스토리·타 워크스페이스 동기화뿐).
dogfood/
