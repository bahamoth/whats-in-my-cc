/target
**/*.rs.bk
.wimcc.sqlite*
payloads/
.DS_Store
webui/node_modules/
webui/dist/

# Node + Vite caches (root-level vitest, webui, anywhere)
node_modules/
.vite/

# superpowers brainstorm mockups
.superpowers/
.wimcc-analysis.sqlite*

# plugin marketplace manifests — un-ignore from the global `.claude*` pattern
# (~/.gitignore); these MUST ship with the repo for `/plugin marketplace add`.
!.claude-plugin/

# serena cache
.serena/

# dogfood 원장은 git 추적 대상 — session-retrospect 스킬의 제안 원장이
# repo 기억을 담당한다 (2026-07-06 정정: 2026-06-23 위생 커밋의 ignore가
# 새 원장만 조용히 누락시켰다. 기존 원장 2건은 이미 추적 중이었음).
